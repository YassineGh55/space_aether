# Optional — only needed when the API runs on a different origin than the web app.
# VITE_API_URL=https://your-api.example.com/api

# Google OAuth client ID (same as GOOGLE_CLIENT_ID on the API)
# VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com
